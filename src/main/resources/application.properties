# Dev
spring.datasource.url=jdbc:postgresql://localhost:5432/MTRS
spring.datasource.username=saahil
spring.datasource.password=Saahil12345

## Prod
#spring.datasource.url=${MTRS_PSQL_URL}
#spring.datasource.username=${MTRS_PSQL_USERNAME}
#spring.datasource.password=${MTRS_PSQL_PASSWORD}
#spring.datasource.driver-class-name=org.postgresql.Driver


spring.sql.init.mode=always

JWT_KEY=${JWT_KEY}
server.port=${MTRS_PORT}

# Github Login
spring.security.oauth2.client.registration.github.client-id=${MTRS_GITHUB_CLIENTID}
spring.security.oauth2.client.registration.github.client-secret=${MTRS_GITHUB_SECRET}

# Google Login
spring.security.oauth2.client.registration.google.client-id=${MTRS_GOOGLE_CLIENTID}
spring.security.oauth2.client.registration.google.client-secret=${MTRS_GOOGLE_CLIENTID}

app.oauth2.redirectUri=http://localhost:5173/oauth2/redirect
logging.level.org.springframework.security=TRACE
